{
  "name": "mindlens",
  "version": "0.1.0",
  "private": true,
  "description": "MindLens Chrome extension for Instagram Web perspective injection",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.292",
    "esbuild": "^0.25.2",
    "typescript": "^5.8.3"
  }
}
